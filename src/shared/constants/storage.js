/**
 * LocalStorage Keys
 * ────────────────
 * Standardized keys used across the application to avoid magic strings.
 */
export const STORAGE_KEYS = {
    TOKEN: 'gz_token',
    THEME: 'gz_theme',
    USER_PREFS: 'gz_user_prefs',
};
